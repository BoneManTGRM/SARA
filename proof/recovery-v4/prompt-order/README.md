# Prompt ordering: unmeasurable with this substitute

Original is an exact B development request after failed-batch evidence. Alternative only reorders top-level components, preserving parsed content, instruction authority and budgets. Current order is canonical alphabetical; alternative puts issue and observations first. Failure evidence, rejected tactics, current patches and permitted actions remain in existing fields. Limits are ceilings; remaining usage is not a distinct prompt field. No change to that interface in this cycle.

The frozen substitute parses JSON by key and ignores issue text, instruction text, strategy text and failure_memory details. Its next action depends on action/test/read observations and its finite domain/patience. It is order-insensitive. No real-model ordering result is claimed. Later evaluate these exact content-identical variants with identical real-model state and seeds if supported, under separately authorized resources.
