# Repository benchmark candidate checkpoint

This is engineering and grading-control evidence, not a SARA benchmark score.
No paid Luna requests have been made and no fresh allowance is activated.

The candidate now contains the real-repository producer, independent kernel
verification, matched twenty-outcome orchestration, Luna token-count/response
adapter, shared conservative budget, and authenticated one-use kernel permits.
The existing durable execution claim holds the entire fresh allowance. No
second ledger, promotion path, historical grant reuse, or Telegram funding was
introduced. Every producer result is frozen before the first official grade.

## Qualification evidence

- Official run `34175065514` passed base/reference controls for task indices
  0, 2 and 3. Combined with previously recorded controls for 1, 4, 6, 8 and 9,
  eight cases have demonstrated the required official outcomes. Remaining
  cases 5 and 7 need the candidate changes qualified in Docker.
- Case 5's image adds one Corepack package-manager field. Normalization is
  restricted to its exact base and observed diff SHA-256
  `c01754d90bb5c2b79bb20813bbe95895999eaa68aa56b0ae62e6c70f75197e18`.
- Case 7's official test needs Postman Echo. The candidate judge proxy allows
  only an HTTPS GET to that fixed fixture. Producers remain offline. The
  official test patch and grading parser are unchanged.
- Public run `34175065536` retained failures for 2, 4 and 5. Candidate fixes
  restore Preact's original Node ABI on a maintained browser OS, build Babel's
  required bundles, and install Docusaurus's locked Sharp native dependency.
- Public case 7's wrapper split a grep expression as shell syntax. Direct
  Mocha invocation preserves it as one argument.
- **The earlier public case 9 pass is invalid.** Run `34173241974` exited zero
  despite esbuild failure, executing only part of the suite. The rerun
  `34174004849` failed with the same underlying issue. The candidate explicitly
  rejects compiler-error logs and resolves the browser import to the existing
  package browser export. No test or assertion is removed.

All original failed artifacts remain evidence. Each new environment fix still
needs its real Docker run; passing offline tests alone does not qualify it.

## Runner and allowance

Read-only command:

```sh
node --import tsx scripts/repository-runner-preflight.ts
```

On the current workspace it reports no Docker daemon, no persistent benchmark
mount, and no model/owner credentials. GitHub Actions can qualify disposable
images, but that does not establish a durable paid benchmark runner.

A concrete proposed ceiling is **$15.60 total**, **$7.80 per arm**, and **$0.78
per attempt**, with at most 50 generations per attempt, 30,000 input tokens and
8,000 output tokens per generation. At the existing frozen $0.20/$1.20 per
million token rates, each maximum request reserves $0.0156. Proposed tool limits
are 200 steps, six public tests, 2 MiB of observed output, and 30 minutes per
attempt. This is a proposal, not an owner grant or invoice estimate.

The exact source, twenty task/arm bindings, public and judge image identities,
producer limits, prices and caps must be frozen on the durable runner before
requesting approval of the final registration. `runRepositoryBenchmark` requires
that configured kernel authorization and its exact authenticated approval. No
production endpoint is activated by this candidate.
