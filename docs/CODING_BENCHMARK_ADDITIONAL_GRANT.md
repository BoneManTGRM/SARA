# One additional matched coding comparison — grant 33d94c9a

## Authorization and scope

The owner explicitly accepted an ADDITIONAL $0.15 TOTAL, equally partitioned
$0.075 per arm, while retaining the original $0.15 unresolved allocation.
Canonical authorization record: BoneManTGRM/SARA#105. New ID:
`33d94c9a-0de6-41d9-a843-fe9880994242`.
The old `41267154-ba42-496a-bb79-1656898ac716` remains blocked, unresolved and
unreconciled. This is not a renewal or replay of the old task or V6/V7/V8 grants.
No recurring run, new infrastructure or NICO/PR69 operation is authorized here.

The new allocation is selected only by the exact nonsecret environment variable
`SARA_CODING_BENCHMARK_AUTHORIZATION_ID`. Unknown values deny. Without it the
original blocked behavior is preserved. GitHub delegation additionally requires
a matching one-hour-or-less permit, exact runtime and workflow revisions,
verified issuer signature and all original owner/kernel checks. The existing
owner launcher writes its exclusive, fsynced one-use claim before spawning the
same matched CLI. Claim collisions, crashes, missing receipts and expiry never
permit another paid invocation. A fresh readiness read reports an existing
launch as unavailable rather than advertising the allocation a second time.

## Frozen task and protocol

Task: `live-free-windows-001`, corpus `sara-live-free-windows-additional-20260905`.
Repair `findFreeWindows` in two writable TypeScript files. Requirements cover
input validation, clipping bookings, unsorted/overlapping/nested/touching ranges,
maximal free half-open intervals, inclusive minimum duration, ordering, and
input immutability. The public task also states the existing sandbox restrictions.
Only model-produced proposals may enter the live candidate; the separately
labeled offline correct fixture is never included in a worker prompt.

Corpus plus protected-test digest:
`71081d5ef6d04c76c88a88c5df87168924a272a17b6339ab3bef104fda6237bf`.
Protected tests contain 31 named runtime tests including 96 fixed-seed calendars
checked in both original and reversed input order by an independent occupancy
oracle. Hidden tests are verifier-owned and excluded from candidate writable
files, prompts and returned failure details. Existing sandbox restrictions are
not relaxed. Before freezing, draft test code was corrected to avoid disallowed
computed indexing and Object.freeze; mutation checks remain. No model calls or
coding-arm results preceded this correction or final freeze.

Preregistered order: active Reparodynamic first, conventional second. Each arm
starts from the same broken baseline with an empty isolated memory context and
up to three attempts. Both use the existing actual OpenAIResponsesClient /
createLunaCodingRepairModel path, gpt-5.6-luna, medium reasoning, full-file output,
identical toolchain/tests and equal ceilings. Both get bounded failed-attempt
lessons. The conventional arm is a capable best-so-far patch/retry/memory control;
the treatment uses the existing active Reparodynamic controller's repair,
selection and failure-learning decisions. Shared SARA policy/verifier/model/
prompt infrastructure means this is not all Reparodynamics versus none.
Compact output, compiler caching and guarded recipe reuse remain off.

Fresh final verification is mandatory. Preserve refusals, invalid proposals,
failed calls, rollbacks, infrastructure errors and incomplete arms. Never rerun
for a favorable outcome; only calculate successful-task speed ratios when both
arms actually complete. One pair does not establish general speed or accuracy.

## Conservative spending and receipts

The entire $0.15 launch is reserved durably before dispatch. Every generation
also has a prior private request reservation. For this new grant those request
reservations cover cache-write input pricing ($0.25 per million input tokens)
and $1.20 per million output tokens: at most $0.0171 for 30,000 input plus 8,000
output tokens. Token-count requests are additionally reserved at up to $0.0075
as if charged for their input, without assuming they are billed. Three counts
and three generations therefore reserve at most $0.0738 per $0.075 arm.
A conservative UTF-8 request-size bound is enforced before token counting,
with 1,024 tokens of additional overhead allowance. The original router's input
count and output caps and the audit's three-request counters also remain.

The standard-rate SARA usage estimate and the conservative cache-write estimate
are labeled separately; neither is a reconciled invoice. Failed or ambiguous
requests retain reservations. No authorization is transferred from a failed arm
to the other. Each trace preserves sanitized request/response IDs, model identity,
usage, output, source snapshots and final verification. No secret is exported.

Rates checked against official primary documentation on 2026-09-05:
https://developers.openai.com/api/docs/models/gpt-5.6-luna
The benchmark requests no optional chargeable hosted tool. API token counting:
https://developers.openai.com/api/docs/guides/token-counting

## Verification and execution boundary

Require unchanged full repository verification, exact-head CI/CodeQL and review
before merge. Confirm actual deployed source and a fresh exact-source permit
before the one launch. Code runs only inside the existing isolated Genome Lab:
no production credential environment, network, writes or arbitrary process
execution. Existing SARA storage and credentials are preserved.

The report must separate arm elapsed time (initial/model/repair/final checks and
its evidence writes), whole CLI time, and setup/CI/deployment/transfer/audit costs.
No historical gains are multiplied; no skipped test is called passed.
