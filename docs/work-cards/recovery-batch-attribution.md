# Recovery batch attribution cycle

Baseline: PR137 379b9a0e5ebbfb26ff65befca0c5b88c4960822b, local be446f7bcf96b51caaa823bbfcd82ddd313981e1. Active producer SHA256 941d5fe29021d1195bda14855c0ed9d7f38ed0e9dc317da2f5ff58eef0dbfd5b. Rejected v3 cap stays isolated.

Observed source: on failed public test, every pending tactic and transition becomes a failed key. Intermediate states have no individual test evidence. Hypothesis: this prevents a useful first edit from being reused after a later edit regresses and the batch rolls back. Development: affine a=0,b=0, target a=2,b=1 fails zero invariant; next target a=2,b=0 requires reusing the first edit. Falsify if B permits that transition, or narrowing attribution fails to unblock it with identical substitute.

One proposed mechanism: associate suppression only with the final transition to the actually tested failed candidate. This establishes observed failed state, not blame for the final edit in all contexts. Keep full raw events and record untested intermediate keys separately. No retry or scope changes. Risk: an untested intermediate prefix can itself be bad; preserve a counterexample and mandatory retained-regression gate.

Primary gate before implementation: >=2 additional resolved tasks across >=2 distinct program families under BOTH responsive and imperfect policies; no lost B-resolved case under ANY policy; no added retained public regression, all negative controls rejected, unchanged authority/accounting/verifier/budgets. Equal-outcome C-B ceilings: +4 requests, +4 tools, +1 test, median time <=1.35*B+50ms, prompt bytes <=1.4*B+4096. Total per-policy requests/tools/tests <=1.35*B. All constraints mandatory; no metric switching. 50 reservations/200 tools/6 tests/2MiB/30min unchanged.

Use exact v3 model and toy program bytes, three policies (patience 1/4/infinity), three counterbalanced repeats. New public requirement descriptions fix prior under-specification, but unchanged substitute ignores semantic requirements and enumerates the supplied finite domain. This limits causal claims. Fault and grader setup common. New evaluation fixtures are assistant-authored; related price variations are one evidence family. All prior fixtures are development only for this cycle. At most three development experiments, one frozen candidate and untouched evaluation. No paid calls or services, NICO, promotion, live pins or SWE-bench activation.

Four methods: minimal batch reproduction; one-variable suppression attribution; critique partial progress vs retained regression and delayed recovery; exact prompt content reordering prepared but order-insensitive substitute cannot measure quality effects. Full npm run verify plus focused red/green. Preserve failed candidate as isolated experiment unless gate passes; no production promotion.
