# Preserve bounded failed-learning evidence

The qualification cycle on `53ed1ef95e7ef14820061e20a7553e3b17ff1ced` made four real model requests across three workflows. X0 returned two proposals rejected for computed access; their exact source disappeared with the runner. X1 reached the 8,192 completion-token ceiling without content. X2 returned HTTP 408 with unavailable usage. No skill qualified. Preserve these outcomes; this change is not included in those comparisons and makes no claim of improved learning.

Confirmed gap: `cloudflare-objective-executor.ts` retains a parsed proposal only in memory, and `cloudflare-self-build.yml` has no bounded evidence upload on failure. A source rejection occurs before the kernel creates an artifact. Thus the next investigation cannot inspect the actual rejected expression or bind it to source bytes.

Smallest change: record each existing generator call, its six-field parsed proposal and source hashes before verification, then a bounded outcome. Preserve those records and the existing Genome Lab candidate directory through the existing GitHub artifact action on successful or failed jobs. This is explicitly opt-in because retaining unverified source is broader than the existing consent to publish a verified draft. The new workflow input defaults to false; the push trigger does not opt in. Never export the kernel event store, environment, credentials, raw provider envelope or reasoning. Partial/interrupted evidence is not acceptance; an overall runner timeout may still prevent upload. Capture failures stop without creating additional repair requests.

Acceptance: an actual kernel source rejection leaves the exact rejected source and producer vectors with matching digests and a rejection outcome; unrelated provider text stays private; existing evidence cannot be overwritten; attempt numbers outside 1–2 and oversized proposals are rejected; no new generation, retry, spending, verifier, authority or promotion behavior. The upload retains at most two bounded proposals and the existing pure-skill artifacts for seven days. Long-term user evidence remains in the requested report/archive.

Falsification: if a real source rejection still loses its captured bytes, if extra provider fields escape, or if a third attempt can be recorded, the repair fails its gate. Use focused tests and the applicable full repository gate. This is a separate Codex-authored diagnostic repair, unqualified against the live workflow until a future authorized invocation. It does not authorize another model request or a merge beyond PR #140.
