# Isolated seeded movement comparator repair

This work qualifies one clearly SYNTHETIC / ISOLATED comparator-repair profile. It neither identifies nor repairs a live Nico's World defect. The implementation agent prepared a copied subset from Nicos-Adventures revision ba0cab4a00664426848c34747f7377e59492f56a. Original reducer SHA-256: 189ded1d872ad290f2d71cdb829c62244126760520646770f27a859b89c70bdc.

The original extensionless import and computed array access are unsupported by the existing Genome Lab guard. Fixture preparation explicitly remaps the import to a protected constants subset, changes the nonnegative array-length lookup to `.at`, and ports the five existing Vitest behaviors to frozen Node assertions. No verifier guard is weakened. Seed preparation changes only the command comparison from `!==` to `===`.

The runtime receives seeded source, exact source metadata, a sole editable path and a frozen regression identity. It accepts only the reviewed reducer shape modulo its three equality tokens. The runtime never receives a corrected replacement. It verifies the baseline, enumerates at most three one-token inversions, validates proposals with existing protected-path/digest controls, and runs each candidate through the existing Genome Lab compiler and permission-restricted child. No model provider or paid-dispatch path is present; model cash is zero and shared infrastructure allocation stays unknown.

A generic runner failure alone is incomplete evidence. Completion requires a candidate to pass the unchanged regression, a fresh independent verifier to pass additional trusted held-out assertions on exactly the same repaired source, and restoration of the original failed source to fail again against the unchanged regression. This causal control supports the narrow seeded comparator diagnosis; individual assertion text is not claimed because the existing verifier returns generic behavioral failure evidence.

The returned result contains exact source/test/artifact identities, attempts, verifier receipts and an isolated reviewable patch. Kernel admission, durable receipt storage, restart/replay and owner-facing production qualification are integration work for the parent change. An optional trusted pre-step callback supports current-authority checks; this engine itself grants no authority. Stop/cancellation may prevent any subsequent child verification.

Acceptance: actual behavioral RED; deterministic GREEN without changing tests or protected source; independent held-out verification; restored RED; original passing comparator stops without a patch; tampered fixtures/protected paths rejected; no model calls or external application effects.

LOCAL implementation evidence: the initial focused command failed with `ERR_MODULE_NOT_FOUND` before the engine existed (exit 1). `node --import tsx --test tests/isolated-software-repair.test.ts` now passes 7/7 (exit 0), including six actual Genome Lab verifications in the successful causal-control path, original no-defect handling, protected-input rejection and current-eligibility stops before/between child executions. `npm run typecheck` passes (exit 0). Completed baseline/attempt evidence survives an eligibility stop; qualification and patch release remain false. Integrated `npm run verify`, exact-head CI/security and authenticated owner runtime proof remain required for parent integration; these local tests do not establish production readiness.

## Owner runtime integration

The existing authenticated owner interface now routes “Prepare an isolated repair for this seeded movement defect.” to goal compilation, procedure lookup and `isolated-defect-repairer`. The controlled fixture is supplied and attributed to IMPLEMENTATION_AGENT; SARA_RUNTIME executes baseline, bounded proposals and fresh verification. Exact admission and current capability/stop/cancellation checks precede each child. This neither admits general repository code nor performs live writes. The protected candidate and patch are retained in the durable receipt; owner UI displays scope and unknown infrastructure allocation.

Focused integration/engine/inventory tests14/14 and existing conversation-worker tests31/31 pass; strict typecheck passes. Authenticated HTTP acceptance executes the actual candidate repair and replay after kernel reboot retains the same receipt. The new actual browser owner gate adds a separate synthetic seeded request after the self-gathered Nico workflow; checks candidate source, frozen regression, causal control, receipt patch hash and same-job replay. Its expected final count is15 ordinary requests; actual browser and exact-head/security qualification remain pending. Production proof must remain harmless, clearly synthetic, and distinct from the empty-material Nico acceptance.
