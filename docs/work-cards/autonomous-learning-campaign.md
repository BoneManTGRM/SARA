# Bounded autonomous learning campaign

Continue ffffba457780fa233fe7cb08858d26ef92eb7df5 under the owner's explicit execution directive. Campaign identity: autonomous-skills-20260908-v1. New allowance: ten total free requests, two per UTC day, two generations per root; prior grants remain consumed and separate. No campaign execution has begun at this work-card checkpoint. Runtime authority requires exact authenticated configuration, not this document.

Implement selection from real missing-capability jobs and owner-frozen learning contracts, campaign reservations in the existing private kernel event store, independently executed acceptance, retained qualification with environment/digest invalidation, and exact-capability routing after existing owner CANARY promotion. Do not expose hidden test answers to generators or alter the historical catalog fixtures. An approved finite contract registry bounds curriculum scope; SARA selects tasks, not implementations.

Minimal case: demonstrate missing campaign accounting/selection/qualification/routing with focused failures. Fixed conditions: frozen contract, deterministic transport doubles only for engineering; never claim live learning from them. Critique: classify each gate separately, preserve repeated failures and uncertain reservations. Reordering: retain existing prompt order initially; test equivalent feedback ordering only if a specific ignored-feedback experiment is authorized and budgeted.

Acceptance: persistent idempotent campaign configuration and restart-safe quota; two concurrent consumers cannot oversubscribe; roots deduplicate across campaigns; selection explains actual gap and value/effort ranking; independent tests reject producer-only success; isolated new-input reuse survives restart and rejects tampered/stale/unpromoted artifacts; revocation and emergency stop deny dispatch/acceptance/invocation. Full repository verification, exact-head CI and CodeQL before merge. Hosted activation must use existing authenticated mandate and provider credentials, without applying unrelated staged changes. Live proof and hosted state remain unverified until observed.

Work boundaries: root owns campaign/selection, kernel, runtime/server integration and their tests/docs. One bounded helper owns only src/learning-qualification.ts and tests/learning-qualification.test.ts. No overlapping edits or new paid resources.

## Implemented engineering boundaries

The worker selects from authorized unmet capability tasks, reserves before dispatch, retains candidate/feedback lineage, records identical source and repeated diagnostics, and gives a child the exact rejected proposal. Autonomous repair uses candidate-then-feedback ordering. A content-equivalence regression preserves the same prompt content; no live comparison or causal improvement is claimed. Model, temperature, seed, maximum tokens and free-only provider are unchanged. At most one child is created; exhausted capabilities are excluded from later selection. These verified outcomes influence subsequent curriculum eligibility, not a claim of measured commercial return.

Independent acceptance expectations stay in the parent process; only input reaches isolated retained code. Qualification binds candidate, frozen contract and environment. A source-isolation regression exposed prototype constructor destructuring; the pure-skill admission guard and runtime dynamic-code prohibition now reject it. PR111 hashes remain intact: the pin test reverses only the explicitly listed diagnostic and pure-skill security deltas. Historical benchmark authorization is not extended to a changed verifier.

Qualified SHADOW remains nonoperational until the existing exact mutation/stage owner promotion. The ordinary job endpoint `POST /api/jobs/:id/execute` takes an input, derives its single capability from the authorized job, selects the saved approved implementation, invokes it, and records output provenance. Successful invocation does not assert the enclosing commercial task passed acceptance. Availability reflects current qualification; reuse failure quarantines the candidate for maintenance. Separate-process tests verify saved retrieval and new inputs, then verifier-change requalification without regeneration or re-promotion. All generated code in those tests is scripted: this is engineering evidence only.

Mandate and emergency-stop event epochs bind generation, qualification and reuse across asynchronous work. A revoke/reactivate or stop on/off cannot restore an in-flight operation's authority. Unknown reservations remain consumed and block ambiguous redispatch. There is no automatic cleanup/refund or replacement-root creation.

## Hosted continuation

The production Railway service remains on deployment b876b8d6-94dc-4623-933e-30f8d4bef328, revision 31752fe68dfaa6ce8c33d145390b576db0693a28 at the recovered checkpoint. Its volume is mounted at /data. The existing 27-variable STAGED patch 0c52b55f-2bbf-4885-9f90-1aa4cdebb9c4 is unrelated and must not be applied. Missing runtime variable names: SARA_AUTONOMOUS_LEARNING_ENABLED, SARA_WORKERS_PLAN, CLOUDFLARE_ACCOUNT_ID, CLOUDFLARE_API_TOKEN. Use secure owner configuration, never extract protected credentials from GitHub or Railway.

Authenticated activation is ordered: inspect GET /api/learning/campaign and the existing standing mandate; reconcile any incompatible mandate explicitly; POST /api/learning/campaign with the private frozen registry and its exact approved digest; activate POST /api/autonomy/learning-mandate only after legitimate owner authentication and explicit current-mandate reconciliation. The existing kernel requires revocation before replacing a different active mandate. Configure the free provider and enable the existing worker. Let its own timer consume the backlog. Do not dispatch the old GitHub qualification workflow as a substitute: it uses a separate store and would not establish this campaign's hosted accounting or autonomy.

Prior catalog run 34291014419 remains failed at TS18048 on both requests. Catalog development, acceptance and saved reuse remain unrun on an eligible live candidate. Component skills must not be labeled Catalog Loss Audit qualified. Final report must separately name engineering results, real-provider results, deployed revision, owner boundaries and conservative campaign accounting.

## Live owner-control repair after PR150 deployment

PR150 deployed to the existing sara-operator production service as c55335a8-9508-43af-bc1c-2fa5874e1e54 on b854daf95fe60d8f66b95aaf962b158569f45de1. Post-merge CI/CodeQL passed. The unrelated 27-variable patch remained staged. Runtime startup logged its pre-existing Luna readiness proof at $0.000015; this was not a learning request or paid learning fallback. Campaign generation remains zero.

Live clicking Owner access had no effect. Browser error: Invalid regular expression: missing /. A minimal regression compiles the actual rendered dashboard script and fails on the same error: the enclosing TypeScript template interpreted the directive newline regex escape. Preserve that failing observation; double-escape only the rendered regex and verify the complete script parses.

The owner dashboard now exposes private campaign review with exact digest approval, persistent quota/runtime status, and the existing internal learning-mandate action. An active conflicting mandate blocks this UI action and still requires explicit existing revocation/reconciliation. Required-capability entry connects ordinary owner directives to the curriculum selector. Authentication remains the existing password-type owner-token form; no secret retrieval, automatic owner impersonation, private contract publication or operational skill promotion is added.
