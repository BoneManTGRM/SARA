# Complete PR85 for owner-authorized integration

Owner request: start completing and merging SARA's existing PR queue while pursuing the highest dependable coding speed. This module is a bounded, inactive building block, not production activation or proof of a live speed multiplier.

Acceptance: retain every verified sibling and known usage if a concurrent fallback throws; keep unknown spending null plus its full reservation; pass the initial physical ceiling to the adapter before dispatch; reject malformed boolean/score verdicts and fractional token counters; settle all parallel sibling calls even for a synchronous exception; preserve four-task/one-fallback limits and the $0.15 ceiling. No deployment, pricing, authority, credential, protected-path or startup code changes.

Five new behavioral cases were first executed against the original implementation: all five failed for the intended missing boundaries. After the repair, a sixth synchronous-exception regression first failed then passed. All eleven focused tests (including five original tests) pass under Node 22.16 native TypeScript stripping. Full committed-lock CI and CodeQL must pass on the published head before merge.

The callback adapter remains responsible for actual pre-request reservation, cancellation and timeouts. The generic scheduler has no provider client or independent financial enforcement authority. A reported overrun is detected and retained, not retroactively undone. The legacy accuracyPreserved field means all supplied tasks passed the supplied verifier; it is not a comparative accuracy claim. No code paths call this new module from SARA production. Error evidence omits provider messages, stack causes and candidate source.
