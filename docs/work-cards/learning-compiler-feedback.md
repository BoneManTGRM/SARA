# Preserve useful compiler feedback for pure skills

Baseline: 541557148f032e95a39674831e8d815e1ac1e711. The authorized free qualification run 34283344837 used two requests and returned the same complete proposal twice. Both failed with twelve TS18046 errors. Source SHA256: 0e8b3ddd0a517eccd71f400cbbda879f47f6d3fff3a771c329d49ef1525cc73f. The strict compiler correctly rejected property access on unknown sort operands at skill.ts lines 52–57. The repair received only an error count.

The response-completeness diagnostic succeeded for this invocation, but the skill did not qualify. Provider compliance and causation are not established by one stochastic run. The six development checks, sixteen acceptance cases and restart/reuse checks remain unrun. Preserve that outcome; do not alter its frozen settings, source or evidence.

Hypothesis: the pure-skill builder discards structured compiler locations that the existing program builder already retains, depriving repair of available rejection evidence. Falsification: an unchanged-source reproduction already delivers the actual TS code and source location through the ordinary repair prompt. Local replay instead confirms only the count.

This offline engineering follow-up changes one mechanism: carry bounded, source-only compiler codes and locations from the existing builder through existing repair feedback and evidence persistence. Reuse GenomeLabTypecheckError; preserve program behavior by default. Include a fixed explanation of the observed TS18046 category, never raw diagnostic messages, source excerpts, test answers, absolute paths or environment details. Keep at most eight valid skill.ts locations and the original error count; unknown errors retain the existing safe fallback.

Acceptance, before implementation: a real kernel type rejection reaches a fake transport's repair prompt and durable rejection receipt with code/location and exact proposal binding; arbitrary diagnostic text and non-skill locations do not leak; feedback stays bounded; the unchanged captured live source still fails strict compilation with all twelve errors and produces no mutation. Existing source policy, behavioral verification, candidate bytes, publication controls, model settings and two-request limit remain unchanged. Focused tests then npm run verify must pass.

One offline development experiment; no new model calls. Do not edit the generated implementation or claim this evidence improvement makes it work. Prepare a reviewable draft PR. Merge and another live diagnostic are outside the consumed invocation's authorization. Preserve content-identical prompt orderings as unrun rather than claiming this code-location change tests ordering. After the offline fix, the next uncertain question is whether the actual model uses specific compiler evidence to change its proposal; behavior and saved reuse still need independent qualification.

Integration finding: the first full gate ran 1,057 tests; 1,056 passed and the historical PR111 byte-identity test rejected the intentional verifier error-metadata delta. No pin or grant was updated. The compatibility test now reverses only those two reviewed metadata edits and requires the original complete-file SHA256. All other pinned files must remain byte-identical; any compiler, policy or test-execution change still fails the historical hash. This maintenance allowance is not authorization to execute an old live experiment on changed source. The original failed gate is retained in the evidence package; rerun focused compatibility checks and the required full gate after this test-only adjustment.
