# Safe recovery finalization and self-build diagnostics

Scope: SARA only. Existing zero-cost resources. No merge, deployment, paid model calls, live grant activation or NICO changes. Keep v1-v4 evidence immutable. PR137 baseline remote 8709d8de1c014db9d14cd9620fae4dc058e10f6a / local 5247e0c26cf6077ff19d557bc44f94529bdee42c.

Research: Anthropic's agent-evaluation guidance separates environmental outcome from transcript claims and capabilities from regression checks. LangGraph persistence documents checkpoints; SWE-agent documents retained trajectories. Apply principles without new dependencies/services. Sources and limitations go in the report.

Recovery diagnosis: v4 narrowing attribution permits useful prerequisites but exports an untested regressing prefix at exhaustion even when a tested passing champion exists. Proposed one additional mechanism relative to v4: choose the already frozen public-passing champion for export whenever termination leaves a different, untested state. No extra model/test/tool calls; sandbox cleanup fences execution. This is rejecting an export, not claiming a physical restore or independent acceptance. Baseline-red tasks without a passing champion retain existing final independent-grading behavior. All rejected candidates remain isolated pending the gate.

Development: prior v4 evaluation becomes inspected regression/development evidence. Reproduce its unsafe prefix, retain all B-resolved cases, budgets and negative controls. One new candidate v5 extends v4 with safe export only; explicitly disclose combined B-to-C change. Common model unchanged; two new public, fully specified string-join and sorting tasks added to the fixed toy application before implementation. These two are untouched evaluation, all older cases are regression replay. No artificial order sensitivity.

Gate before implementation: both new task families improve verified resolution over B under responsive and imperfect policies. No B-resolved loss or new public regression on ANY policy in the prior 15-case regression replay or new evaluation. Negatives stay rejected. Identical 50 reservations/200 tools/6 public tests/2MiB/30min. Equal-outcome overhead ceilings: +4 requests/+4 tools/+1 test, median <=1.35B+50ms, prompt <=1.4B+4096 bytes; total requests/tools/tests <=1.35B. One candidate, one untouched run, no post-evaluation tuning.

Production screenshot: task 7c3a6b53-35c1-42a3-9dc5-83988f5aa7c2 failed on workflow 34160079894 at main 9c763cf36efbc548774725fa318acbe8dad2e7e6. Digest confirms repository verification exit1, output length251426; raw child output was discarded, so not all historical failing checks can be recovered. Current main 5a1144cb795789964d54d6fb8bcded9fb90462c0 retains the same executor/publisher/workflow bytes. Clean checkout lacks pinned native checker and its test fails ENOENT. Install and validate existing pinned checker before claiming a task, without weakening tests or increasing the workflow ceiling. Retain safe structured failure stage/digest; do not leak raw output or retry uncertain result recording.

Site source: /workspace/sites/sara-dashboard at 9b6fc426e4fd69ef7330bb979336bce95496a2e8, saved version49. Owner-authenticated live API exposes only curated safe metadata. Add safe failure summaries and trusted workflow links; show disconnected/paused/expired/failed state accurately, dates, and readable text. Browser feed is unsupported, so disable the misleading browser action and state actual capabilities. Never fabricate browser images, clear failure history or retry the task automatically. Site source changes are reviewable, not deployment authorization.

Validation: focused failing/passing tests, fixed-budget comparison, full SARA verification gate, site build and owner-authentication/data-projection tests. Publish reviewable evidence. Actual live recovery remains unproven until changes are activated and a separately authorized task completes.
