# PR57: fair comparison and one-use execution

## Frozen integration inputs

Current main: `f547b92ad6b55ec5fe50d2ae657442495c7592ac`.
PR57 gate repairs: `60206bc105c107f5c9eee9c859a4fa1e41a2113a`.
Both histories must remain ancestors. The integrated source, not either older branch in isolation, must pass the locked gate.

## Falsifiable acceptance criteria

Both arms receive the same maximum three model calls, bounded last-two attempt lessons, best-so-far candidate retention, exact duplicate protection, protected paths, model route and spend ceilings. Both can finish on the second or third proposal. A correct-first run executes initial, candidate and fresh final verification. Unstable final acceptance never becomes completion. Caller mutations cannot expand admitted limits or change the second arm's frozen inputs. Unknown methods, unsafe token counts and unsupported execution labels fail closed. Unspecified execution is simulated.

A private durable execution claim must precede every paid arm of the manual CLI. Concurrent processes admit only one invocation. Success, failure, malformed claim files and process termination do not reset the claim. Missing receipts are uncertainty, not permission to repeat model work. Historical evidence cannot be adopted as permission to resume spending. A new paid experiment requires fresh exact-source owner approval; this integration issues none.

Arm persistence and summary validation share boolean and safe-integer checks. Existing complete-corpus, 80% quality floor, regression, cost and recommendation-recomputation safeguards remain intact.

## Scope and nonclaims

The conventional control deliberately shares SARA's lesson representation and prompt machinery. The result is a conservative same-framework controller comparison, not all Reparodynamics versus none, and not proof of a uniquely Reparodynamic effect. Historical one-proposal receipts are not reclassified as fair-control evidence. Source and controller identity changes prevent pooling them with the new implementation.

The new CLI is `benchmark:coding:evidence:live`; the existing live benchmark script and entrypoint remain byte-identical to main. No production, startup, credentials, provider route, prices, authority, Constitution, protected tests, dependency lock, compact defaults or compiler-cache settings change. The new workflow only verifies and archives exact source. It has no provider credential or deployment action. CodeQL's reporting permission cannot modify source.

## Verification record

Before implementation, the compatibility-integrated runner passed its four existing tests. The fifteen new fairness/boundary tests produced fourteen expected failures and one pass. A behavior-neutral execution-guard seam produced five expected failures before implementation. Preserve these local RED logs separately from the final CI artifact.

Local focused tests use Node 22.16.0 native type stripping. Local TypeScript is 5.8.3, not the committed 5.9.3 dependency; this is provisional verification only. The authoritative complete gate must run `npm ci --ignore-scripts`, `npm run verify`, the V7 scripted harness and a repeated HTTP test on the immutable integrated head. Archive exact source, commit/tree identities and command exit receipts. No new paid model run or speed claim is part of this work card.

The one-use claim assumes the same retained owner-private local filesystem. Privileged deletion, copied ledgers, network filesystems and payment-provider exactly-once guarantees are out of scope. An interrupted experiment remains blocked; it is not automatically resumed.
