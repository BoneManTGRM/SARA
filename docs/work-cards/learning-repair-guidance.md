# Make failed-skill repair requirements actionable

Baseline afe42dff8a32314d30e43367e32cb11f3fc8e18d. Live run 34287159286 returned the same 2,299-byte proposal twice. Both attempts failed TS18048 at skill.ts:40:27 and 45:27. The bounded feedback includes locations but no meaning for TS18048. The repair prompt asks for a replacement without explicitly distinguishing TypeScript assertions from input validation or rechecking the whole public contract. Source review also finds missing input validation, altered SKU matching and a missing price blank-overwrite branch. No acceptance case has been run on that rejected source.

Scope: src/cloudflare-free-generator.ts, focused tests, and this work card. Do not edit the generated catalog implementation, kernel, verifier, frozen acceptance fixtures, model configuration, workflow, retry count, budget or authority. No model invocation or merge is included in this engineering slice's evidence.

Experiment 1: add fixed safe meaning for the observed TS18048 diagnostic. A real Map.get compiler rejection must supply an explanation that undefined values need narrowing through the actual repair request and durable candidate-bound evidence. Before the change the explanation is absent. Privacy limits and the eight-location cap remain unchanged. This is evidence-presentation repair, not proof the model will respond.

Experiment 2: add one repair-only instruction block requiring runtime validation of the stated input contract, reconsideration of all public requirements after a compiler fix, and a changed candidate for a source defect. Preserve the previous proposal as untrusted data, the initial prompt, and all request settings. Test the actual transport request and real kernel rejection of a model double that ignores the instructions. Do not supply a correct implementation or let a fake decide the winner. If ignored, the original rejection remains in force; no additional request is made.

Component reordering: retain a content-identical alternative with feedback after the rejected proposal in the evidence package; do not change the production ordering or claim an effect from a deterministic transport double. Reflection: distinguish repaired guidance, model responsiveness, independently correct behavior, retained reuse, and profit. The latter four remain unverified unless actual evidence demonstrates them.

Engineering acceptance: baseline regression fails for the missing TS18048 meaning; real-kernel integration passes with bounded meaning and exact proposal-bound evidence; first-request content/settings remain unchanged; ignored repair is rejected with zero mutations and no fallback; focused tests and npm run verify pass. Any live qualification must still pass the original six development and sixteen untouched acceptance cases, saved source/digest restart, new-input reuse, and ordinary authorized invocation. Reject all claims of skill qualification from scripted response tests.
