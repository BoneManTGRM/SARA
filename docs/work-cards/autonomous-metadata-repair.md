# Autonomous follow-up for bounded metadata failures

User asks to ensure SARA can learn skills on her own. The existing delegated learning worker reserves requests, recalls failures, queues one repair and retains verified SHADOW skills. Confirmed gap after PR148: precise metadata feedback exists, but the follow-up selector excludes it.

Scope: share the exact fixed metadata classifier with the existing kernel follow-up selection; regression and documentation. Preserve all authority, mandate, positive declared value, candidate binding, daily two-request ceiling, lineage, emergency-stop and promotion gates. No new curriculum, paid provider or unlimited recursion. Live deployment requires the existing authenticated mandate; do not replace a commercial mandate.

Minimal case: a real 438-character metadata rejection must queue one unchanged-contract child. Baseline fails because no child exists. Fixed conditions: same proposal and worker path, reboot, repeated invalid proposal; assert one child, unchanged contract, retained 300-character feedback, only two requests, no third generation. Critique: deterministic tests establish unattended control flow, not model learning. Reordering: keep prompt and model settings unchanged to isolate follow-up eligibility. Original catalog fixtures remain frozen.

Acceptance: baseline failure reproduced; focused autonomous/feedback tests and npm run verify pass; exact messages only; arbitrary exceptions remain excluded; no operational promotion. Separately record live model and hosted activation evidence without conflating them with local tests.
