# Owner-requested new real SARA comparison

Owner explicitly requested a new real run after a73c932b, rejecting simulation as final evidence.

Acceptance: one new live Luna comparison through deployed SARA HTTP/server/kernel with empty isolated memory per arm, all planned jobs and attempts retained, fresh authoritative and independent kernel checks, exact source and OIDC launcher binding, a new one-use maximum $0.15 total / $0.05 per arm, no consumed grant replay, and detailed proof/report. No change to coding algorithm, acceptance, verifier, customer jobs, unrelated integrations, or recurring infrastructure. Preserve current deployed main f087528417c14bdaed173faf38f1920044d9d9e9.

Qualification: focused grant/real-only/no-replay tests, full npm run verify, independent review, CI and CodeQL, exact-tree merge and deployment. Paid launch only after exact fresh unused preflight; one POST, GET-only recovery. Automated local fixtures are safety tests, never the requested live evidence.

The new versioned runner copies the qualified PR127 protocol and implementation, changing only grant/import bindings. Historical runner and pin files remain byte-identical to the base; historical runners fail closed on changed authority source.

Typecheck exit 0 and focused 22/22 tests passed. Independent read-only review found no blockers: 49 pins match, seven historical runner/grant/CLI/pin files are unchanged, live rejects injected providers, and capped one-use claims remain mandatory. Full repository verification and external CI/CodeQL must complete before merge.
