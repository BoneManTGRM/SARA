# Bounded autonomous learning preparation

Owner outcome: learn useful skills and execute learning work without an instruction for each attempt. Current main is 85f641ac158495e5491fa3a5eb39802fc6049d5b (tree aae4ae95219f024490c11a9f30ff7f530736350b). The latest live diagnostic failed proposal parsing; exact output is unavailable. Do not claim a specific malformed-output cause or successful learning.

Confirmed executable gaps: the free generator receives recalled memory but sends only its digest; failed self-builds record a job reason but no reusable failure memory. The runtime has no consumer of an explicitly delegated learning backlog. Fix evidence continuity first, then add a bounded consumer using existing jobs, mandate policy, private event store and Genome Lab verifier. Do not add another backend or authority.

Acceptance before implementation: (1) distinct malformed/ambiguous parse failures retain safe counts and provider completion metadata, without raw content or reasoning; (2) a real rejected candidate produces bounded kernel failure evidence that survives restart and appears in a later generator prompt; (3) a queue consumer selects an authorized zero-cost learning job, reserves before dispatch, makes at most two requests/day and one request/job, never replays an uncertain reservation, and respects mandate revocation and emergency stop; (4) verified candidates remain SHADOW. All unchanged repository verification must pass. A fixture generator proves plumbing, not real model learning. The catalog's six development and sixteen acceptance cases remain untouched until a real eligible candidate exists.

Four methods: minimal reproductions; separate evidence and queue slices under fixed conditions; reflect on generator-supplied versus kernel-verified behavior; retain content-identical prompt-order variants if real-model comparison cannot be performed. No live call is needed for engineering tests. Previous invocation grants remain consumed. Persistent automatic activation requires an authenticated standing mandate for internal/skill-learning, free provider configuration and deployment of reviewed source. Do not replace an existing commercial mandate or promote generated code automatically.

Research: Cloudflare's GLM-4.7-Flash documentation supports the existing OpenAI-compatible endpoint and completion ceiling; requested seeds are only best effort. It does not establish why the historical response failed: https://developers.cloudflare.com/workers-ai/models/glm-4.7-flash/ . Preserve current model/settings until a controlled live comparison supports a change.

## Recursive-learning extension

The owner requested recursive learning and broader durable memory while retaining the income mission. The Constitution already puts sustainable distributable owner wealth at the center; do not rewrite it or financial authority. Extend the same PR with one evidence-driven follow-up for a failed, positive-declared-value learning objective when an actual source/compiler/behavioral rejection supplies actionable feedback. Preserve the objective, acceptance criteria and zero budget exactly. Persist lineage, prevent duplicate children across restart, and stop after two total generations per root. Unknown provider/parsing failures do not justify blind recursion. All descendants share the existing two-reservation daily cap and mandate. Record successful kernel-verified SHADOW artifacts in durable skill memory with digests and explicit limits; memory is not promotion or external acceptance.

Falsification: a third generation, duplicate child, changed objective/test contract, recursion after revocation or unknown failure, or a SHADOW memory presented as operational authority rejects the extension. Fixture responsiveness is plumbing evidence only. Keep extensive durable history separate from small relevant prompt context. Actual profitability, a self-directed curriculum and live-model learning remain unqualified.

## Shared paid-model reservation control

Owner requested autonomy within a chosen price. Existing owner-assistant receipts are written after dispatch and the runtime creates separate paid clients for coding; these cannot independently enforce a shared crash-safe ceiling. Add a kernel-owned, authenticated-owner-configured monthly allocation with zero default. Before any runtime OpenAI generation, count the exact input and atomically reserve the full token-ceiling cost using owner-reviewed rates. Reservations are durable and non-refundable in this conservative first version, including failed/uncertain requests. Keep observed usage separate from reserved allowance; no savings claim. Opening charges must explicitly cover earlier current-month usage outside the new control. No setting is activated in this engineering cycle.

Falsification: dispatch without configuration, raising the limit without authenticated approval, concurrent oversubscription, restart resetting a reservation, emergency-stop dispatch, or a runtime paid client bypassing the shared gate. This limits SARA-accounted token spend under configured rates, not other applications using the key or provider invoice timing. No hosted tools/storage are enabled. Owner must supply the dollar amount and review prices before activation.
